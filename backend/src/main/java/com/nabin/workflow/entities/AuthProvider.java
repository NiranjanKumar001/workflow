package com.nabin.workflow.entities;

public enum AuthProvider {
    LOCAL,      // Email/Password registration
    GOOGLE,     // Google OAuth
    FACEBOOK,   // Facebook OAuth (for future)
    GITHUB      // GitHub OAuth (for future)
}